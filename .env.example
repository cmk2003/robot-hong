# ========================================
# 小虹 - 情感陪伴机器人 环境配置
# ========================================

# LLM 提供商配置（二选一）
# 可选值: qwen, deepseek
LLM_PROVIDER=qwen

# 千问 API Key（使用千问时必填）
# 获取地址: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# DeepSeek API Key（使用 DeepSeek 时必填）
# 获取地址: https://platform.deepseek.com/
# DEEPSEEK_API_KEY=your_deepseek_api_key_here

# ========================================
# 服务配置
# ========================================

# 运行环境: development / production
ENV=production

# Gradio 服务端口
GRADIO_SERVER_PORT=7860

# 数据库路径
DATABASE_PATH=./data/emotional_bot.db

# 日志级别: DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO

# 是否启用多agent
AGENT_MODE=multi

# 各 Agent 模型配置（可选，不设置则使用默认模型）
AGENT_DEFAULT_MODEL=qwen-plus     # 所有 Agent 的默认模型
AGENT_EMOTION_MODEL=qwen-turbo    # 情感分析用小模型（省成本）
AGENT_MEMORY_MODEL=qwen-turbo     # 记忆检索用小模型
AGENT_RESPONSE_MODEL=qwen-max     # 回复生成用大模型（保证质量）
AGENT_SAVE_MODEL=qwen-turbo       # 记忆保存用小模型
AGENT_REVIEW_MODEL=qwen-plus      # 质量评审用中等模型