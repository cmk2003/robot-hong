# 核心依赖
openai>=1.0.0              # LLM客户端（兼容千问/DeepSeek）
gradio>=4.44.0,<5.0.0      # Web界面（Python 3.9 需要 4.x 版本）
gradio-client>=1.3.0,<2.0.0  # Gradio 客户端（版本匹配）
python-dotenv>=1.0.0       # 环境变量管理

# LangChain（新增）
langchain>=0.3.0           # LangChain 核心
langchain-openai>=0.2.0    # OpenAI 兼容模型
langgraph>=0.2.0           # LangGraph 状态图

# 数据库
# SQLite 内置，无需额外依赖

# 开发依赖
pytest>=7.0.0              # 测试框架
pytest-asyncio>=0.21.0     # 异步测试支持

